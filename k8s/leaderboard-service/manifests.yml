apiVersion: apps/v1
kind: Deployment
metadata:
  name: leaderboard-service
  namespace: cricheroes
spec:
  replicas: 2
  selector:
    matchLabels:
      app: leaderboard-service
  template:
    metadata:
      labels:
        app: leaderboard-service
    spec:
      containers:
      - name: leaderboard-service
        image: cricheroes/leaderboard-service:latest
        ports:
        - containerPort: 3012
        envFrom:
        - configMapRef:
            name: leaderboard-service-config
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: leaderboard-service
  namespace: cricheroes
spec:
  selector:
    app: leaderboard-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3012
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: leaderboard-service-hpa
  namespace: cricheroes
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: leaderboard-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
